base_config: ../markets/tw_day_trade_100m.yaml

# Immutable inference contract for the restored TWD 100 million fold-11
# checkpoint.  Keep all 99 saved features even when a separate research config
# excludes slower official datasets.
experiment_name: tw-day-trade-initial-capital-100m-fold11-deployment

runner:
  output_dir: artifacts/markets/tw_day_trade_100m
  resume: false

data:
  feature_exclude: []
