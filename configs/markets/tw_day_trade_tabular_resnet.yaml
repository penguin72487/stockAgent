base_config: tw_day_trade_model_experiments_common.yaml

experiment_name: tw-day-trade-tabular-resnet-lookback32-v1
runner:
  output_dir: artifacts/model_experiments/tw_day_trade/tabular_resnet

training:
  model_name: tabular_resnet
  lookback: 32
  batch_size_train: 32
  tabular_resnet:
    embedding_dim: 128
    hidden_dim: 256
    n_blocks: 4
    dropout: 0.15

